var expect = require('expect.js'),
    fxdownload = require('..');

describe('fxdownload', function() {
  it('should do something', function(done) {
    //expect(fxdownload()).to.equal('Hello, world');
    // TODO: add meaningful tests :-)
    done();
  });
});
